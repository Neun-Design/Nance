# @nance/spec is build tooling only (tsx, vitest, tsc): there is no "production install"
# of this package, so devDependencies are always required. This overrides the npm
# default of omit=dev that NODE_ENV=production would otherwise trigger.
include=dev
